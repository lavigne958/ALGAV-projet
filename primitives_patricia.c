#define _XOPEN_SOURCE 700

#include <stdlib.h>
#include <stdio.h>
#include <string.h>


/* 
descr:  predicat qui determine si un noeud possede des fils  
param:  node* arbre -> Patricia_Tries ou on va faire notre recherche  
retour: 1 si arbre possede un fils, 0 sinon */
int possede_fils(node* arbre){
  if(arbre(*fils))
    /*
      racine[(int)'a'].fils[i] = (node*) malloc(sizeof(node));
      *(racine + (int) 'a').*(fils+i)
    */
    /*
#define PREFIX_MAX 20
#define NB_CHAR_MAX 127
#define EPSILON '_'

typedef struct node{
    char prefix[PREFIX_MAX];
    struct node** fils;
}node;
    */
