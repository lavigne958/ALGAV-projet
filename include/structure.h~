#ifndef _STRUCTURE_H
#define _STRUCTURE_H

#define PREFIX_MAX 20
#define NB_CHAR_MAX 127
#define EPSILON '_'

typedef struct node{
  char prefix[PREFIX_MAX];
  int size;
  struct node** fils;
}node;

#endif

